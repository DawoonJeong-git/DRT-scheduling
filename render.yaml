services:
  - type: web
    name: gantt-dispatch-api
    env: docker
    plan: free
    dockerfilePath: ./server/Dockerfile
    dockerContext: ./server
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: PORT
        value: 5056